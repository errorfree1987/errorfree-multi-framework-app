# Railway: set service "Root Directory" to admin-dashboard (this folder).
# https://docs.railway.app/deploy/builds

[build]
builder = "nixpacks"

[deploy]
startCommand = "next start -p ${PORT:-3000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
